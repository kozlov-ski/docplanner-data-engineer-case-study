[project]
name = "nomly-dispatch-slice"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = [
    "dbt-core>=1.10,<2",
    "dbt-postgres>=1.10,<2",
    "pika>=1.3,<2",
    "psycopg[binary]>=3.2,<4",
    "jsonschema[format]>=4.23,<5",
]

[tool.uv]
package = false
